<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Tracking Results</title>
  <style>
    body {
      font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Helvetica, Arial, sans-serif;
      background-color: #f4f6f8;
      color: #212b36;
      display: flex;
      justify-content: center;
      min-height: 100vh;
      margin: 0;
      padding: 20px;
      box-sizing: border-box;
    }
    .container {
      width: 100%;
      max-width: 600px;
      margin-top: 20px;
    }
    .card {
      background: white;
      padding: 30px;
      border-radius: 8px;
      box-shadow: 0 0 0 1px rgba(63, 63, 68, 0.05), 0 1px 3px 0 rgba(63, 63, 68, 0.15);
      margin-bottom: 20px;
    }
    .header {
      display: flex;
      justify-content: space-between;
      align-items: flex-start;
      margin-bottom: 24px;
      flex-wrap: wrap;
      gap: 10px;
    }
    .tracking-number {
      font-size: 20px;
      font-weight: 600;
      color: #212b36;
      margin: 0;
    }
    .carrier {
      font-size: 14px;
      color: #637381;
      margin-top: 4px;
    }
    .status-badge {
      display: inline-flex;
      align-items: center;
      padding: 4px 12px;
      border-radius: 12px;
      font-weight: 600;
      font-size: 13px;
      text-transform: uppercase;
      background-color: #e3f2fd; /* Default blue */
      color: #1565c0;
    }
    .status-badge.delivered {
      background-color: #e4f8f0;
      color: #008060;
    }
    .status-badge.transit {
      background-color: #fff4e5;
      color: #b98900;
    }
    .status-badge.exception {
      background-color: #fbeae5;
      color: #d82c0d;
    }

    .timeline {
      position: relative;
      padding-left: 24px;
      border-left: 2px solid #dfe3e8;
      margin-top: 10px;
    }
    .event {
      position: relative;
      margin-bottom: 30px;
      padding-left: 24px;
    }
    .event:last-child {
      margin-bottom: 0;
    }
    .event::before {
      content: '';
      position: absolute;
      left: -31px; /* Adjust to center on the line */
      top: 2px;
      width: 12px;
      height: 12px;
      background: #dfe3e8;
      border-radius: 50%;
      border: 2px solid #fff;
    }
    .event.latest::before {
      background: #008060;
      box-shadow: 0 0 0 4px rgba(0, 128, 96, 0.2);
    }
    .event-time {
      font-size: 13px;
      color: #637381;
      margin-bottom: 4px;
    }
    .event-desc {
      font-size: 15px;
      color: #212b36;
      line-height: 1.5;
    }
    .event-location {
      font-size: 13px;
      color: #637381;
      margin-top: 2px;
    }
    
    .back-btn {
      display: inline-block;
      text-decoration: none;
      color: #637381;
      font-size: 14px;
      font-weight: 500;
      display: flex;
      align-items: center;
      margin-bottom: 20px;
      transition: color 0.2s;
    }
    .back-btn:hover {
      color: #212b36;
    }
    .back-btn svg {
      width: 16px;
      height: 16px;
      margin-right: 6px;
    }

    /* Mobile adjustments */
    @media (max-width: 600px) {
      .card {
        padding: 20px;
      }
      .header {
        flex-direction: column;
        align-items: flex-start;
      }
      .status-badge {
        margin-top: 8px;
      }
    }
  </style>
</head>
<body>

<div class="container">
  <a href="/apps/track" class="back-btn">
    <svg viewBox="0 0 20 20" fill="currentColor">
      <path fill-rule="evenodd" d="M9.707 16.707a1 1 0 01-1.414 0l-6-6a1 1 0 010-1.414l6-6a1 1 0 011.414 1.414L5.414 9H17a1 1 0 110 2H5.414l4.293 4.293a1 1 0 010 1.414z" clip-rule="evenodd" />
    </svg>
    Back to Search
  </a>

  <div class="card">
    <div class="header">
      <div>
        <h1 class="tracking-number"><%= tracking %></h1>
        <div class="carrier"><%= carrier || 'Unknown Carrier' %></div>
      </div>
      
      <% 
        let badgeClass = '';
        let statusLower = (status || '').toLowerCase();
        if (statusLower === 'delivered') {
          badgeClass = 'delivered';
        } else if (statusLower === 'transit' || statusLower === 'in transit') {
          badgeClass = 'transit';
        } else if (statusLower === 'exception' || statusLower === 'expired') {
          badgeClass = 'exception';
        }
      %>
      <div class="status-badge <%= badgeClass %>">
        <%= status %>
      </div>
    </div>

    <div class="timeline">
      <% if (!events || events.length === 0) { %>
        <p style="color: #637381; font-style: italic;">No tracking updates available yet.</p>
      <% } else { %>
        <% events.forEach((event, index) => { %>
          <div class="event <%= index === 0 ? 'latest' : '' %>">
            <div class="event-time"><%= event.time %></div>
            <div class="event-desc"><%= event.description %></div>
            <% if (event.location) { %>
              <div class="event-location"><%= event.location %></div>
            <% } %>
          </div>
        <% }); %>
      <% } %>
    </div>
  </div>
</div>

</body>
</html>
